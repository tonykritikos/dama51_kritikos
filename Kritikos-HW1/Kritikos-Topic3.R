#bring a table with table with absolute frequencies of cyl and am
table(mtcars$cyl, mtcars$am)

#make a histogram for the wt attribute with 5 bars showing
hist(mtcars$wt, breaks=5)

#make a boxplot for the attribute wt
boxplot(mtcars$wt)
 